# SafeTrack (EF-SF-002)

แอปบันทึกผลตรวจอุปกรณ์ความปลอดภัย/อุปกรณ์ฉุกเฉิน ~195 จุด แล้วออกรายงานเป็นไฟล์ PowerPoint

## สถาปัตยกรรม

**ทั้งแอปคือ `index.html` ไฟล์เดียว** — ไม่มี build step, ไม่มี package.json, ไม่มี framework,
ไม่มี test runner, ไม่มี linter เปิดไฟล์ด้วยเบราว์เซอร์แล้วใช้งานได้เลย

โครงสร้างภายในไฟล์เรียงตามลำดับนี้:

| ส่วน | เนื้อหา |
|---|---|
| `<script>` #1 | `window.__loadPptx()` — โหลด pptxgenjs จาก CDN แบบ lazy พร้อม SRI hash |
| `<style>` | CSS ทั้งหมด ธีมเขียว CPF (light theme) |
| `<body>` | โครง UI: topbar / stats / progress / search / `#list` / FAB / bottom sheet |
| `<script>` #2 | error box |
| `<script>` #3 | `window.TEMPLATE_DATA` — รายการจุดตรวจทั้งหมด (บรรทัดยาวมาก 1 บรรทัด) |
| `<script>` #4 | ตรรกะทั้งหมดของแอป |

## ⚠️ เรื่องที่ต้องรู้ก่อนแก้อะไรก็ตาม

### id มี 2 ชั้น — อย่าสับสน

```js
uid(si,pi) === 's{ดัชนีหมวด}p{ดัชนีจุด}'          // ใช้ใน DOM / event เท่านั้น
STABLE[uid] === [category, code, location, order].join('\x01')  // ใช้เก็บลงดิสก์
```

`uid` อิงตำแหน่งในอาร์เรย์ ถ้าเพิ่ม/ลบจุดกลางรายการ uid ของทุกจุดหลังจากนั้นจะเลื่อนหมด
จึงต้องเก็บลงดิสก์ด้วย **stable id** แทน (ตัวคั่นคือ `\x01` ไม่ใช่สตริงว่าง — มองไม่เห็นตอนอ่านโค้ด)

**การแก้ `TEMPLATE_DATA` = stable id เปลี่ยน = แอปหาข้อมูลเดิมของจุดนั้นไม่เจอ**
ข้อมูลจะไม่ถูกลบทิ้ง (ดู `orphanMeta` / `orphanPhotoKeys` ด้านล่าง) แต่จะไม่แสดงบนหน้าจอ
ถ้าจำเป็นต้องแก้ template จริง ๆ ให้บอกผู้ใช้สำรองข้อมูลก่อน และเตรียมทางย้ายข้อมูลไว้ด้วย

### ต้องสมมติเสมอว่า storage อาจใช้ไม่ได้

ผู้ใช้จริงเปิดแอปนี้ผ่าน **เบราว์เซอร์ในแอป (CPF Connext, LINE)** ซึ่งอาจบล็อก `localStorage`
หรือไม่ให้ใช้ `IndexedDB` โค้ดจึงต้องมีทางถอยเสมอ ห้ามเรียก `localStorage` ตรง ๆ
ให้ใช้ `lsGet` / `lsSet` ที่ถอยไปใช้ `memStore` ในแรมให้อัตโนมัติ

## ชั้นเก็บข้อมูล

| ที่เก็บ | คีย์ | เนื้อหา |
|---|---|---|
| localStorage | `safetrack_ef002_meta_v2` | ผลตรวจ + หมายเหตุ (เล็ก) map จาก stable id |
| localStorage | `safetrack_ef002_sett_v1` | ตั้งค่า UI (autoNext / onlyPhoto / filter) |
| localStorage | `safetrack_ef002_v1` | **legacy** ก้อนรวมเก่า มีโค้ด `migrateOld()` ย้ายให้ครั้งเดียวแล้วลบทิ้ง |
| IndexedDB | `safetrack` → store `photos` | รูป JPEG เป็น Blob คีย์ด้วย stable id |

รูปต้องอยู่ IndexedDB เพราะ localStorage มีเพดาน ~5MB ซึ่งไม่พอกับรูป ~195 ใบ (หลายสิบ MB)

### ตัวแปรสถานะหลัก

```js
meta            // uid -> {result, note}     ผลตรวจของจุดที่ template ปัจจุบันรู้จัก
orphanMeta      // stable id -> {...}        ข้อมูลของ sid ที่ไม่รู้จัก — เขียนกลับทุกครั้งที่ save
photoKeys       // Set<uid>                  "จุดไหนมีรูป" — แหล่งความจริง ไม่ใช่ photoURL
photoBlob       // uid -> Blob               cache ชั่วคราว หรือที่เก็บจริงเมื่อไม่มี IndexedDB
photoURL        // uid -> object URL         เฉพาะรูปที่กำลังแสดง จำกัดด้วย LRU
orphanPhotoKeys // stable id[]               รูปใน IDB ที่ template ไม่รู้จักแล้ว
collapsedCats   // Set<ดัชนีหมวด>            หมวดที่ถูกยุบ — ห้ามอ่านจาก DOM
```

**สถานะ UI ที่ต้องอยู่รอด `render()` ให้เก็บเป็นตัวแปร ไม่ใช่อ่านจาก DOM**
`render()` สร้าง `.cat` / `.card` ใหม่ทั้งหมดทุกครั้งที่ค้นหาหรือเปลี่ยนตัวกรอง
สถานะที่เก็บไว้ใน class ของ element จะหายทันที (`collapsedCats` มีไว้แก้ปัญหานี้)

**`orphanMeta` และ `orphanPhotoKeys` มีไว้กันข้อมูลหาย** — `saveMeta()` เขียนทับทั้งคีย์
ถ้าไม่รวม orphan กลับเข้าไปด้วย การแก้ template หนึ่งครั้งจะลบผลตรวจเดิมทิ้งถาวร

### การโหลดรูปแบบ lazy

ตอนเปิดแอปจะอ่าน **เฉพาะคีย์** จาก IndexedDB (`idbAllKeys()`) ไม่อ่าน Blob
ตัว Blob จะถูกอ่านเมื่อการ์ดเลื่อนเข้ามาในจอเท่านั้น ผ่าน IntersectionObserver → `ensurePhotoURL()`
และคง object URL ไว้ไม่เกิน `MAX_LIVE_URL` (30) ใบ ตัวที่ไม่ได้อยู่ในจอจะโดน `releaseURL()`

ผลลัพธ์: รูป 120 ใบ (~35MB) เดิมตรึงไว้ในแรมทั้งหมด ตอนนี้เหลือ ~15 ใบ (~4.4MB)

**ถ้าต้องอ่าน Blob ในโค้ดใหม่ ให้ใช้ `readPhotoBlob(uid)`** อย่าอ่าน `photoBlob[uid]` ตรง ๆ
เพราะส่วนใหญ่จะไม่มีค่าอยู่ในแรม (export กับ backup ใช้ helper ตัวนี้)

## การส่งไฟล์ออกจากแอป

**ห้ามยิง `<a download>` ตรง ๆ ในโค้ดใหม่** — เบราว์เซอร์ในแอปมักเมินคำสั่งนี้เงียบ ๆ
ผู้ใช้จะเห็นว่า "สำเร็จ" ทั้งที่ไม่ได้ไฟล์ ให้เรียก `offerFile(blob, ชื่อไฟล์)` แทน
ซึ่งเปิดแผงให้ผู้ใช้เลือกเองระหว่าง **ส่งไฟล์** (Web Share) กับ **ดาวน์โหลด**

```
สร้างไฟล์ (นานหลายวินาที) ──> offerFile() ──> ผู้ใช้แตะปุ่ม ──> share() / download()
```

**ต้องแยกเป็น 2 จังหวะแบบนี้เสมอ** เพราะ `navigator.share()` ต้องถูกเรียกใต้
transient user activation (~5 วินาที) แต่การสร้าง ZIP/pptx ใช้เวลานานกว่านั้น
ถ้าเรียก `share()` ต่อท้าย `await` ยาว ๆ จะได้ `NotAllowedError`

กติกาของแผงนี้: **ห้ามสลับทางให้ผู้ใช้เองเงียบ ๆ** — ส่งไม่สำเร็จให้บอกแล้วชี้ไปปุ่มดาวน์โหลด
และ `AbortError` (ผู้ใช้ปิดแผงแชร์เอง) ไม่ใช่ข้อผิดพลาด

`pptx.write({outputType:'blob'})` คืน type เป็น `application/zip` ต้องหุ้ม Blob ใหม่
ด้วย MIME ของ PowerPoint ไม่งั้นแอปปลายทางตอนแชร์จะเห็นเป็นไฟล์ zip

## สไตล์โค้ด

- ES6 ธรรมดา ไม่มี module / bundler — ทุกอย่างเป็น global ในสโคปเดียว
- ฟังก์ชันสั้น ๆ เขียนติดกันบรรทัดเดียวเป็นปกติของไฟล์นี้ (โดยเฉพาะ helper ของ IndexedDB)
- ข้อความ UI ทั้งหมดเป็นภาษาไทย คอมเมนต์ผสมไทย/อังกฤษได้
- สร้าง HTML ด้วยการต่อสตริง — **ข้อมูลจากผู้ใช้ต้องผ่าน `esc()` เสมอ**
- pptxgenjs โหลดจาก CDN เมื่อกด export เท่านั้น พร้อม SRI hash
  ถ้าเปลี่ยนเวอร์ชันไลบรารี **ต้องคำนวณ `integrity` ใหม่** ไม่งั้นเบราว์เซอร์จะปฏิเสธไฟล์

### เลขหน้าใน PowerPoint — ห้ามนับเองระหว่างสร้าง

ตารางสรุปใช้ `autoPage:true` ซึ่ง **pptxgenjs แตกสไลด์เพิ่มเอง** ตัวนับที่เพิ่มทีละสไลด์
จึงเพี้ยนทั้งเล่ม (เคยเป็นบั๊กจริง: ไฟล์มี 36 สไลด์ แต่เลขจบที่ 34)
ต้องประทับเลขหน้า **ตอนท้ายครั้งเดียว** โดยวนบน `pptx.slides` ก่อน `writeFile()`
และรายงานจำนวนสไลด์จาก `pptx.slides.length` เท่านั้น

## การทดสอบ

ไม่มีชุดทดสอบในรีโป ทดสอบด้วยมือ:

```bash
python3 -m http.server 8000     # แล้วเปิด http://localhost:8000
```

สิ่งที่ควรตรวจทุกครั้งที่แตะชั้นเก็บข้อมูล:

1. กดปกติ/ไม่ปกติ + พิมพ์หมายเหตุ + ใส่รูป → reload → ข้อมูลต้องอยู่ครบ
2. สำรองข้อมูล → กู้คืน → ต้องได้ข้อมูลเดิมกลับมาครบ
3. ออก PowerPoint → รูปต้องขึ้นครบทุกสไลด์ (เส้นทางนี้พึ่ง `readPhotoBlob` ที่อ่านจาก IDB)
4. บล็อก localStorage / IndexedDB ใน DevTools → แอปต้องยังใช้งานได้และขึ้นแบนเนอร์เตือน

ถ้าจะเขียนสคริปต์ทดสอบด้วย Playwright: ตัวแปรระดับบนสุดเป็น `const`/`let` จึงเป็น lexical global
เรียกใน `page.evaluate` ด้วยชื่อเปล่า ๆ (`photoKeys`) ได้ แต่ `window.photoKeys` จะเป็น undefined

## ไฟล์สำรองข้อมูล (ZIP v3)

```
safetrack-backup-YYYY-MM-DD.zip
├── meta.json          {version:3, app, ts, meta:{sid:{result,note}}, photos:[{file,id}]}
└── photos/0001.jpg …  JPEG ดิบ ไม่ผ่าน base64
```

ชื่อ entry เป็นเลขลำดับ **ไม่ใช่ stable id** เพราะ stable id มี `\x01` กับภาษาไทยอยู่ข้างใน
การจับคู่กลับเก็บไว้ใน `photos[]` ของ `meta.json`

`makeZip` / `readZip` / `crc32` เขียนเองในไฟล์ (โหมด STORE ไม่บีบอัด)
**ห้ามเปลี่ยนไปใช้ไลบรารีจาก CDN** — การสำรองข้อมูลต้องทำงานตอนออฟไลน์ได้ นั่นคือเหตุผลที่มันมีอยู่
JPEG บีบอัดมาแล้ว STORE จึงไม่เสียอะไร และไฟล์เล็กกว่าแบบ base64 เดิม ~25%

**ฝั่งกู้คืนห้ามอ่านไฟล์ทั้งก้อนเข้าแรม** — `readZip` อ่านแค่สารบัญ แล้วคืนเนื้อแต่ละ entry
เป็น `Blob.slice()` ซึ่งเป็นมุมมอง ไม่ใช่สำเนา วัดจริงกับไฟล์ 35MB: JS heap พุ่ง +1.5MB
(รูปแบบ JSON+base64 เดิมพุ่ง +48MB และโตตามขนาดไฟล์)

**ไฟล์สำรองเก่าต้องกู้คืนได้เสมอ** — ดู magic byte `PK\x03\x04` เพื่อแยกทาง
ถ้าไม่ใช่ ZIP ให้ไปเส้นทาง JSON เดิม (v2 และ legacy) ซึ่งต้องคงไว้

**ลำดับสำคัญ:** ต้อง parse สารบัญ + `meta.json` ให้ผ่านก่อน **แล้วค่อย `clearAll()`**
ไม่งั้นไฟล์สำรองที่เสียจะทำให้ผู้ใช้เสียข้อมูลเดิมไปด้วย

## ข้อจำกัดที่รู้อยู่แล้ว

- ไฟล์สำรองใช้ ZIP32 จึงจำกัดที่ 65,535 entry และ 4GB (งานจริง 195 รูป ~75MB ห่างมาก)
  ถ้าต้องเกินนี้ต้องทำ ZIP64 เพิ่ม
- ต้องมีอินเทอร์เน็ตตอนกดออก PowerPoint (โหลด pptxgenjs จาก CDN)
- `navigator.storage.persist()` ช่วยลดโอกาสที่เบราว์เซอร์ลบข้อมูลเอง แต่ไม่รับประกัน
  จึงยังต้องเตือนผู้ใช้ให้สำรองข้อมูลอยู่ดี
