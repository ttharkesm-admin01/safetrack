# คู่มือการใช้งาน SafeTrack (EF-SF-002)

คู่มือนี้สำหรับ **ผู้ใช้งาน/ผู้ตรวจ** — ใช้บันทึกผลตรวจอุปกรณ์ความปลอดภัยและอุปกรณ์ฉุกเฉิน
**28 หมวด รวม 195 จุด** แล้วออกรายงานเป็นไฟล์ PowerPoint
(ถ้ากำลังส่งมอบงานให้คนใหม่ ข้ามไปอ่าน [ส่งมอบงานให้ผู้รับช่วง](#11-ส่งมอบงานให้ผู้รับช่วง) ได้เลย)

> 📱 **เปิดคู่มือนี้ในตัวแอปได้เลย** — กดปุ่ม **"คู่มือ"** มุมขวาบนของหัวแอป
> เนื้อหาเดียวกันฉบับย่อ อ่านได้ทันทีหน้าอุปกรณ์แม้ไม่มีสัญญาณ

---

## สารบัญ

1. [สรุปการใช้งานใน 1 นาที](#1-สรุปการใช้งานใน-1-นาที)
2. [เปิดแอปอย่างไรให้ข้อมูลไม่หาย](#2-เปิดแอปอย่างไรให้ข้อมูลไม่หาย)
3. [ทัวร์หน้าจอ](#3-ทัวร์หน้าจอ)
4. [ตรวจ 1 จุด ทำอะไรบ้าง](#4-ตรวจ-1-จุด-ทำอะไรบ้าง)
5. [ค้นหา ตัวกรอง และการยุบหมวด](#5-ค้นหา-ตัวกรอง-และการยุบหมวด)
6. [ออกรายงาน PowerPoint](#6-ออกรายงาน-powerpoint)
7. [สำรองข้อมูล / กู้คืนข้อมูล](#7-สำรองข้อมูล--กู้คืนข้อมูล)
8. [เมนู ⋯ ตัวเลือกเพิ่มเติม](#8-เมนู--ตัวเลือกเพิ่มเติม)
9. [แถบเตือนสีเหลือง/แดง แปลว่าอะไร](#9-แถบเตือนสีเหลืองแดง-แปลว่าอะไร)
10. [ปัญหาที่พบบ่อย](#10-ปัญหาที่พบบ่อย)
11. [ส่งมอบงานให้ผู้รับช่วง](#11-ส่งมอบงานให้ผู้รับช่วง)
12. [ข้อจำกัดที่ต้องรู้](#12-ข้อจำกัดที่ต้องรู้)

---

## 1. สรุปการใช้งานใน 1 นาที

```
เปิดแอปด้วย Safari/Chrome  →  เดินตรวจ: 📷 ถ่ายรูป + กด ✓ ปกติ หรือ ✕ ไม่ปกติ (+ พิมพ์หมายเหตุถ้าไม่ปกติ)
   →  กด "ออก PowerPoint"  →  เลือก "ส่งไฟล์" หรือ "ดาวน์โหลด"
   →  เมนู ⋯ → "สำรองข้อมูล" ทุกครั้งที่ตรวจเสร็จ
```

กติกาสำคัญ 3 ข้อ:

1. **ข้อมูลเก็บอยู่ในเครื่องของคุณเท่านั้น** ไม่มีเซิร์ฟเวอร์ ไม่มีการซิงก์ ล้างข้อมูลเบราว์เซอร์ = ข้อมูลหาย
2. **สำรองข้อมูลเป็นนิสัย** ไฟล์สำรอง 1 ไฟล์ = ผลตรวจ + รูปทั้งหมด ย้ายเครื่องได้ด้วยไฟล์นี้
3. **เปิดด้วยเบราว์เซอร์จริง (Safari/Chrome)** ไม่ใช่เบราว์เซอร์ในแอป LINE/CPF Connext

---

## 2. เปิดแอปอย่างไรให้ข้อมูลไม่หาย

| สิ่งที่ควรทำ | เหตุผล |
|---|---|
| เปิดด้วย **Safari** (iPhone) หรือ **Chrome** (Android) | เบราว์เซอร์ในแอป LINE/CPF Connext อาจไม่ให้บันทึกข้อมูลถาวร |
| ถ้าเปิดจากลิงก์ในแชท ให้กดเมนูแล้วเลือก **"เปิดในเบราว์เซอร์"/"Open in Safari"** ก่อนเริ่มตรวจ | ถ้ายังอยู่ในแอปแชท จะขึ้นแถบเตือนสีเหลืองว่าข้อมูลจะหายเมื่อปิดหน้า |
| ใช้ **เครื่องเดียว เบราว์เซอร์เดียว** ตลอดรอบการตรวจ | ข้อมูลผูกกับเบราว์เซอร์นั้น ๆ ไม่ข้ามเครื่องเอง |
| เพิ่มลงหน้าจอโฮม (Add to Home Screen) หรือ Bookmark ไว้ | หาลิงก์เดิมได้เสมอ ไม่ต้องไปรื้อในแชท |
| ห้ามกดล้างประวัติ/ล้างข้อมูลเว็บไซต์ ระหว่างรอบการตรวจ | เป็นสาเหตุอันดับ 1 ที่ข้อมูลหาย |

**เรื่องสัญญาณ:** เมื่อหน้าเว็บโหลดขึ้นมาแล้ว ทุกอย่างทำงานได้โดยไม่ต้องมีเน็ต — บันทึกผล ใส่รูป
ออก PowerPoint และสำรองข้อมูล ทำได้ครบขณะเดินในโรงงานที่ไม่มีสัญญาณ
แต่ **ระหว่างที่ไม่มีสัญญาณ อย่ากดรีเฟรชหรือปิดแท็บ** เพราะการโหลดหน้าใหม่ยังต้องดึงไฟล์จากเน็ต
(ข้อมูลที่บันทึกแล้วไม่หาย แต่จะเปิดแอปไม่ได้จนกว่าจะมีสัญญาณ)

---

## 3. ทัวร์หน้าจอ

```
┌──────────────────────────────────────────────┐
│ ST  SafeTrack รายงานตรวจอุปกรณ์               │  ← หัวแอป
├──────────────────────────────────────────────┤
│  195      120        70        5              │  ← 4 ช่องสถิติ = ปุ่มกรองด้วย
│ ทั้งหมด ยังไม่ตรวจ  ปกติ   ไม่ปกติ             │
├──────────────────────────────────────────────┤
│ ▓▓▓▓▓▓▓░░░░░  ตรวจแล้ว 75 จาก 195 จุด (38%)  │  ← ความคืบหน้า
├──────────────────────────────────────────────┤
│ 🔍 ค้นหา รหัส / ตำแหน่ง / ประเภท…             │
├──────────────────────────────────────────────┤
│ ▼ ถังดับเพลิงชนิดผงเคมีแห้ง  ปกติ 18  20 จุด  │  ← หัวหมวด (แตะเพื่อยุบ/ขยาย)
│   ┌────────┐ ┌────────┐ ┌────────┐            │
│   │  รูป   │ │  รูป   │ │  รูป   │            │  ← การ์ดจุดตรวจ
│   └────────┘ └────────┘ └────────┘            │
└──────────────────────────────────────────────┘
        [ ออก PowerPoint ]  [ ⋯ ]                 ← ปุ่มลอยด้านล่าง
```

**4 ช่องสถิติด้านบน** แสดงยอดรวม และ **แตะเพื่อกรองรายการ** ได้ (แตะซ้ำเพื่อเลิกกรอง)

**การ์ดจุดตรวจ 1 ใบ** ประกอบด้วย

- ช่องรูป + ป้ายสถานะมุมรูป: **รอตรวจ** (เทา) / **ปกติ** (เขียว) / **ไม่ปกติ** (แดง)
- ลำดับ + รหัสอุปกรณ์ เช่น `1. A-001` และตำแหน่ง เช่น `โรงพักขยะ 1`
- ปุ่ม `📁 เลือกรูป` `📷 ถ่าย` และ `✕` (ลบรูป — โผล่เมื่อมีรูปแล้ว)
- ปุ่ม `✓ ปกติ` และ `✕ ไม่ปกติ`
- ช่องหมายเหตุ (พิมพ์แล้วยืดตามข้อความ)

จุดที่กด "ไม่ปกติ" การ์ดจะขึ้นเป็นสีแดงเพื่อให้เห็นชัดตอนไล่ดูซ้ำ
ปุ่ม `↑` มุมล่างจะโผล่เมื่อเลื่อนลงไปไกล ๆ สำหรับกลับขึ้นบนเร็ว ๆ

---

## 4. ตรวจ 1 จุด ทำอะไรบ้าง

1. **ใส่รูป** — กด `📷 ถ่าย` เพื่อถ่ายใหม่ หรือ `📁 เลือกรูป` เพื่อเลือกจากคลังภาพ
   (แตะที่ช่องรูปว่าง ๆ ก็เท่ากับ "เลือกรูป")
   รูปจะถูกย่อให้กว้างไม่เกิน 1200px และแปลงเป็น JPEG อัตโนมัติเพื่อประหยัดพื้นที่
   รูปถ่ายแนวตั้งจะไม่หมุนเอง (แอปอ่านค่าการหมุนจากไฟล์ให้แล้ว)
2. **กดผลตรวจ** — `✓ ปกติ` หรือ `✕ ไม่ปกติ`
   - **กดปุ่มเดิมซ้ำ = ยกเลิกผล** กลับไปเป็น "รอตรวจ" (ใช้ตอนกดผิด)
3. **พิมพ์หมายเหตุ** — จำเป็นเมื่อ "ไม่ปกติ" เพราะข้อความนี้จะไปโผล่ในรายงานช่อง
   *"อธิบายสภาพที่ไม่ปกติ"* ให้เขียนสั้น ๆ ตรงประเด็น เช่น `เข็มเกจตกโซนแดง`, `หลอดไม่ติด`
   หมายเหตุบันทึกเองหลังหยุดพิมพ์ครู่เดียว ไม่ต้องกดเซฟ

**Auto Next:** ค่าเริ่มต้นเปิดอยู่ — พอกดผลตรวจแล้วหน้าจอจะเลื่อนไปจุดถัดไปให้เอง
(กรอบเขียวจะกระพริบที่การ์ดใหม่) ถ้าไม่ชอบ ปิดได้ในเมนู ⋯

**ดูรูปใหญ่:** แตะที่รูปที่มีอยู่แล้ว → เปิดเต็มจอ แตะอีกครั้งเพื่อปิด
**ลบรูป:** กด `✕` ข้างปุ่มถ่ายรูป (มีถามยืนยัน)

> ผลตรวจและหมายเหตุบันทึกทันที ไม่มีปุ่ม "บันทึก" และไม่ต้องกดอะไรก่อนปิดแอป

---

## 5. ค้นหา ตัวกรอง และการยุบหมวด

| ต้องการ | วิธี |
|---|---|
| หาอุปกรณ์ตัวหนึ่ง | พิมพ์รหัส ตำแหน่ง หรือชื่อประเภทลงช่องค้นหา เช่น `A-031`, `หอพัก`, `ไฟฉุกเฉิน` |
| ดูว่าเหลือจุดไหนยังไม่ตรวจ | แตะช่อง **ยังไม่ตรวจ** ด้านบน (ตรวจครบจะขึ้น "ตรวจครบทุกจุดแล้ว 🎉") |
| ไล่ดูเฉพาะจุดที่ไม่ปกติ ก่อนออกรายงาน | แตะช่อง **ไม่ปกติ** |
| เลิกกรอง | แตะช่องเดิมซ้ำ หรือแตะช่อง **ทั้งหมด** |
| ย่อรายการให้เลื่อนหาหมวดง่าย | แตะหัวหมวดเพื่อยุบ/ขยาย หรือเมนู ⋯ → **ยุบ/ขยายทุกหมวด** |

ตัวกรอง คำค้นหา และหมวดที่ยุบไว้ จะถูกจำไว้เมื่อเปิดแอปครั้งถัดไป
(ถ้าเปิดมาแล้วรายการดูน้อยผิดปกติ ให้ดูว่ามีตัวกรองค้างอยู่หรือเปล่า)

> **ตัวกรองมีผลกับหน้าจอเท่านั้น ไม่มีผลกับรายงาน** — ออก PowerPoint ได้ทุกจุดเสมอ

---

## 6. ออกรายงาน PowerPoint

กดปุ่มสีส้ม **"ออก PowerPoint"** ด้านล่าง แล้วรอ — ปุ่มจะบอกความคืบหน้า
(`กำลังสร้าง… 120/195`) ประมาณไม่กี่วินาทีถึงหลายสิบวินาทีตามจำนวนรูป **ห้ามปิดหน้าจอระหว่างนี้**

ไฟล์ที่ได้ชื่อ `รายงานอุปกรณ์ฉุกเฉิน-YYYY-MM-DD.pptx` ประกอบด้วย

1. **สไลด์แรก — ตารางสรุป** 1 บรรทัดต่อ 1 ประเภทอุปกรณ์ มีคอลัมน์
   `อุปกรณ์ที่ตรวจ / จำนวนที่มี / ปกติ / ไม่ปกติ / อธิบายสภาพที่ไม่ปกติ`
   ช่องขวาสุดคือหมายเหตุของจุดที่กด "ไม่ปกติ" (ขึ้นเป็น `รหัส: หมายเหตุ`)
   ถ้าตารางยาวเกิน 1 หน้า PowerPoint จะแตกหน้าต่อให้เองพร้อมหัวตารางซ้ำ
2. **สไลด์รูปรายจุด** หมวดละชุด สไลด์ละ 14 รูป (7 คอลัมน์ × 2 แถว)
   ใต้รูปเป็นลำดับ + รหัส + ตำแหน่ง จุดที่ยังไม่มีรูปจะเป็นกรอบเทาเขียนว่า `(ใส่รูปภาพ)`
3. **เลขหน้า** มุมขวาล่างทุกสไลด์

เมื่อสร้างเสร็จ จะขึ้นแผง **"ไฟล์พร้อมแล้ว"** บอกชื่อไฟล์กับขนาด และมี 2 ทางเลือก

- **ส่งไฟล์** — เปิดแผงแชร์ของเครื่อง ส่งเข้า LINE / อีเมล / บันทึกลง Files ได้เลย
  (ปุ่มนี้จะโผล่เฉพาะเครื่องที่รองรับ)
- **ดาวน์โหลด** — บันทึกลงโฟลเดอร์ดาวน์โหลดของเครื่อง

> ถ้ากด "ส่งไฟล์" แล้วขึ้นว่าส่งไม่สำเร็จ **ให้กด "ดาวน์โหลด" แทน** ไฟล์ยังอยู่ ไม่ต้องสร้างใหม่
> ถ้าเผลอปิดแผงไป ต้องกดสร้างใหม่

**ตัวเลือก "เลือกออกเฉพาะจุดที่มีรูป"** (เมนู ⋯) — เปิดไว้จะได้รายงานที่มีแต่จุดที่ถ่ายรูปแล้ว
เหมาะกับตอนส่งรายงานระหว่างทาง ถ้าปิด (ค่าเริ่มต้น) จะได้ครบ 195 จุด โดยจุดที่ยังไม่มีรูปเป็นกรอบเปล่า

**เรื่องฟอนต์:** รายงานตั้งค่าเป็น Angsana New ถ้าเปิดบนเครื่องที่ไม่มีฟอนต์นี้
ตัวอักษรอาจเลื่อน/ขนาดเพี้ยนได้ — ตรวจไฟล์บนเครื่องที่ใช้ส่งจริงก่อนส่งออกไป

---

## 7. สำรองข้อมูล / กู้คืนข้อมูล

**เรื่องสำคัญที่สุดในคู่มือนี้** — ข้อมูลอยู่ในเครื่องเท่านั้น ไฟล์สำรองคือสำเนาชุดเดียวที่มี

### สำรองข้อมูล

เมนู ⋯ → **สำรองข้อมูล** → รอปุ่มนับ `กำลังสำรอง… 80/195` → แผง "ไฟล์พร้อมแล้ว" → ส่ง/ดาวน์โหลด

ได้ไฟล์ `safetrack-backup-YYYY-MM-DD.zip` ข้างในมีผลตรวจ หมายเหตุ และรูปทั้งหมด
(รูป 195 ใบราว 70–80MB) เก็บไว้ที่ที่หายยาก เช่น อีเมลตัวเอง, OneDrive, Google Drive

**ควรสำรองเมื่อ**

- ตรวจเสร็จทุกครั้งที่ปิดงานประจำวัน
- ก่อนกด "กู้คืนข้อมูล" หรือ "ล้างข้อมูลทั้งหมด"
- ก่อนเปลี่ยนเครื่อง/ล้างเครื่อง/อัปเดตระบบเครื่อง
- ทันทีที่เห็นแถบเตือนสีเหลืองด้านบน

### กู้คืนข้อมูล

เมนู ⋯ → **กู้คืนข้อมูล** → เลือกไฟล์ `.zip` → ยืนยัน (จะบอกว่าในไฟล์มีรูปกี่รูป) → รอปุ่มนับจนเสร็จ

> ⚠️ **การกู้คืนเขียนทับข้อมูลในเครื่องทั้งหมด** ของเดิมที่ยังไม่ได้สำรองจะหายถาวร
> ถ้าไฟล์สำรองเสียหรือไม่ใช่ของ SafeTrack แอปจะฟ้องและ **ไม่แตะข้อมูลเดิม** ให้เห็นข้อความ
> "กู้คืนข้อมูลแล้ว" เท่านั้นถือว่าสำเร็จ

ไฟล์สำรองรุ่นเก่าที่เป็น `.json` ยังกู้คืนได้ปกติ

---

## 8. เมนู ⋯ ตัวเลือกเพิ่มเติม

| ปุ่ม | ทำอะไร |
|---|---|
| **เลื่อนไปจุดถัดไปอัตโนมัติ (Auto Next)** | เปิด/ปิดการเลื่อนหน้าจอไปจุดถัดไปหลังกดผลตรวจ (ค่าเริ่มต้น: เปิด) |
| **เลือกออกเฉพาะจุดที่มีรูป** | ให้รายงาน PowerPoint ตัดจุดที่ยังไม่มีรูปออก (ค่าเริ่มต้น: ปิด) |
| **ยุบ / ขยายทุกหมวด** | ยุบทุกหมวดพร้อมกัน ถ้ายุบอยู่แล้วทั้งหมดจะขยายทั้งหมด |
| **สำรองข้อมูล** | สร้างไฟล์ ZIP ผลตรวจ + รูปทั้งหมด (ดูข้อ 7) |
| **กู้คืนข้อมูล** | เอาไฟล์สำรองกลับเข้าแอป — **เขียนทับของเดิมทั้งหมด** |
| **ล้างรูปกำพร้า (n รูป)** | โผล่เฉพาะเมื่อมีรูปค้างจากรายการจุดตรวจรุ่นก่อน ลบได้เพื่อคืนพื้นที่ — ถ้าไม่แน่ใจ ให้สำรองข้อมูลก่อน |
| **ล้างข้อมูลทั้งหมด** (แดง) | ลบผลตรวจและรูปทุกอย่างในเครื่อง ใช้ตอนเริ่มรอบตรวจใหม่ **สำรองข้อมูลก่อนเสมอ** |
| **บรรทัด "พื้นที่เก็บข้อมูล: …"** | บอกว่าใช้พื้นที่ไปเท่าไรจากโควตา และมีรูปกี่ใบ ถ้าเกิน 80% จะเป็นสีเตือน |

---

## 9. แถบเตือนสีเหลือง/แดง แปลว่าอะไร

แถบข้อความที่โผล่เหนือช่องค้นหา บอกสุขภาพของที่เก็บข้อมูล — **ไม่ควรมองข้าม**

| ข้อความที่เห็น | ความหมาย | ต้องทำอะไร |
|---|---|---|
| ⚠️ ตัวเปิดนี้ไม่อนุญาตให้บันทึกถาวร | กำลังเปิดในเบราว์เซอร์ที่บล็อกการบันทึก (มักเป็นในแอป LINE/Connext) | สำรองข้อมูลทันที แล้วย้ายไปเปิดด้วย Safari/Chrome |
| ⚠️ เบราว์เซอร์นี้เก็บรูปภาพถาวรไม่ได้ | ผลตรวจเซฟได้ แต่ **รูปจะหายเมื่อปิดหน้า** | สำรองข้อมูลก่อนปิดหน้า แล้วเปลี่ยนเบราว์เซอร์ |
| ⚠️ บันทึกลงเครื่องไม่สำเร็จ (พื้นที่อาจเต็ม) | เครื่องเต็ม เซฟล่าสุดไม่สำเร็จ | สำรองข้อมูลทันที แล้วเคลียร์พื้นที่เครื่อง |
| ℹ️ เบราว์เซอร์อาจลบข้อมูลนี้เองหากไม่ได้เปิดแอปนานหลายวัน | เป็นคำเตือนปกติ ไม่ใช่ข้อผิดพลาด | สำรองข้อมูลหลังตรวจเสร็จทุกครั้ง |

ถ้ามี **แถบสีแดงพร้อมข้อความภาษาอังกฤษ (JS ERROR / PROMISE ERROR)** ที่ขอบบนสุด
นั่นคือแอปมีข้อผิดพลาด — ถ่ายภาพหน้าจอเก็บไว้ทั้งข้อความ แล้วส่งให้ผู้ดูแลแอป

---

## 10. ปัญหาที่พบบ่อย

**เปิดมาแล้วรายการว่าง / ตัวเลขเป็น 0**
แอปจะขึ้นข้อความวินิจฉัยให้เอง ให้ลองรีเฟรชเมื่อมีสัญญาณ ถ้ายังไม่หายให้ส่งข้อความนั้นให้ผู้ดูแล

**กดดาวน์โหลดแล้วไม่ได้ไฟล์**
มักเกิดในเบราว์เซอร์ในแอปแชท ให้ใช้ปุ่ม **"ส่งไฟล์"** แทน หรือย้ายไปเปิดด้วย Safari/Chrome

**"ส่งไฟล์ไม่สำเร็จ — ลองกดดาวน์โหลดแทน"**
ทำตามที่บอก กด "ดาวน์โหลด" ในแผงเดิม ไฟล์ยังอยู่ ไม่ต้องสร้างใหม่

**"ยกเลิกการส่ง — ไฟล์ยังอยู่ เลือกใหม่ได้"**
ไม่ใช่ข้อผิดพลาด แค่ปิดแผงแชร์ไป กดส่งหรือดาวน์โหลดใหม่ได้เลย

**ผลตรวจหาย/รูปหาย**
เช็คตามลำดับ: (1) มีตัวกรองค้างอยู่ไหม (2) เปิดเบราว์เซอร์ตัวเดิมหรือเปล่า
(3) มีใครล้างข้อมูลเบราว์เซอร์ไหม ถ้าหายจริงให้กู้คืนจากไฟล์สำรองล่าสุด

**พื้นที่ใกล้เต็ม / เซฟรูปไม่ได้**
สำรองข้อมูลออกก่อน แล้วลบไฟล์/แอปอื่นในเครื่องให้ว่าง ถ้าเป็นรอบตรวจที่ปิดงานแล้ว
ให้สำรองเก็บไว้แล้ว "ล้างข้อมูลทั้งหมด" เพื่อเริ่มรอบใหม่

**รูปในรายงานหมุนผิดด้าน**
แอปแก้การหมุนให้ตอนใส่รูปแล้ว ถ้ายังเจอ ให้ลบรูปนั้นแล้วถ่าย/ใส่ใหม่

**กดออก PowerPoint แล้วค้างนาน**
ปกติถ้ามีรูปหลายร้อยใบ ดูตัวเลขความคืบหน้าบนปุ่มว่าขยับอยู่ไหม ระหว่างนี้อย่าสลับแอปทิ้งไว้นาน

---

## 11. ส่งมอบงานให้ผู้รับช่วง

ทำตามลำดับนี้ในวันส่งมอบ

- [ ] **ปิดรอบข้อมูลให้เรียบร้อย** — ตรวจให้ครบ (ใช้ตัวกรอง "ยังไม่ตรวจ" เช็ค) และออกรายงาน PowerPoint รอบล่าสุดเก็บไว้
- [ ] **สำรองข้อมูล** เมนู ⋯ → สำรองข้อมูล แล้วส่งไฟล์ `.zip` ให้ผู้รับช่วง + เก็บสำเนาไว้อีกที่
- [ ] **ส่งลิงก์แอป** ที่ใช้อยู่จริง พร้อมย้ำให้เปิดด้วย Safari/Chrome และ Bookmark ไว้
- [ ] **ส่งคู่มือนี้** (ไฟล์ `USER-GUIDE.md` หรือส่งลิงก์หน้านี้)
- [ ] **ให้ผู้รับช่วงลองกู้คืนบนเครื่องตัวเอง** — เมนู ⋯ → กู้คืนข้อมูล → เลือกไฟล์ `.zip` ที่ให้ไป
      แล้วเช็คว่าตัวเลขสถิติ 4 ช่องตรงกับเครื่องเดิม **นี่คือขั้นที่ยืนยันว่าส่งมอบสำเร็จจริง**
- [ ] **สอนใช้งาน 5 นาที** ตามข้อ 4 (ถ่ายรูป → ปกติ/ไม่ปกติ → หมายเหตุ), ข้อ 6 (ออกรายงาน),
      ข้อ 7 (สำรองข้อมูล) และย้ำ 3 กติกาในข้อ 1
- [ ] **บอกว่าใครดูแลตัวแอป** ถ้าต้องแก้รายการจุดตรวจ (เพิ่ม/ลด/เปลี่ยนรหัส-ตำแหน่ง)
      ต้องให้คนที่ดูแลโค้ดทำ และ **ต้องสำรองข้อมูลก่อนแก้ทุกครั้ง** เพราะการแก้รายการ
      อาจทำให้ผลตรวจเดิมของจุดนั้นไม่แสดงบนหน้าจอ (รายละเอียดทางเทคนิคอยู่ใน `CLAUDE.md`)

**คำแนะนำสำหรับผู้รับช่วงวันแรก:** เอาไฟล์สำรองที่ได้มากู้คืนลงเครื่องตัวเอง แล้วสำรองออกมาอีกรอบ
เพื่อพิสูจน์ว่าทั้งวงจร (กู้คืน → ตรวจ → สำรอง) ใช้งานได้บนเครื่องของตัวเองจริง

---

## 12. ข้อจำกัดที่ต้องรู้

- **ไม่มีเซิร์ฟเวอร์ ไม่มีบัญชีผู้ใช้ ไม่มีการซิงก์** — 2 เครื่องที่เปิดแอปเดียวกันคือ 2 ชุดข้อมูลแยกกัน
  การรวมข้อมูลทำได้ทางเดียวคือสำรอง/กู้คืน (และการกู้คืนเขียนทับ ไม่ใช่รวมข้อมูล)
- **ไม่มีถังขยะ/เลิกทำ** ลบรูปหรือล้างข้อมูลแล้วคือหาย กู้ได้จากไฟล์สำรองเท่านั้น
- **รายการจุดตรวจถูกกำหนดไว้ในตัวแอป** เพิ่ม/ลบจุดจากหน้าจอไม่ได้ ต้องแก้ที่ตัวแอป
- เบราว์เซอร์อาจลบข้อมูลของเว็บเองถ้าไม่ได้เปิดนานหลายวันหรือพื้นที่เครื่องเต็ม
  แอปขอยกเว้นให้แล้วแต่ไม่รับประกัน — **ไฟล์สำรองคือสิ่งเดียวที่รับประกันได้**
